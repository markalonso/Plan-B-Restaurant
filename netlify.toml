[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Environment variables are set in Netlify UI, not here
  # This prevents secrets from being exposed in build logs
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
